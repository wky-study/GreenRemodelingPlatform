<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.metarial">

	<select id="getMaterialList"
		parameterType="com.team.green.material.dto.MaterialDTO"
		resultType="com.team.green.material.dto.MaterialDTO">
		SELECT
		item_name
		, item_id
		, item_model
		, item_brand
		, item_img
		, item_effi_level
		, item_effi_stat
		, item_type
		, item_size
		FROM material
		WHERE 1=1
		AND item_id = #{itemId}
	</select>

</mapper>
